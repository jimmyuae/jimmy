# Deploy Jimmy on Koyeb + Supabase

Use this only if Koyeb lets you use a free instance without payment.

The permanent data is stored in Supabase, not Koyeb. Koyeb only runs the Node.js app.

Use the same environment variables described in `SUPABASE_PERMANENT_STORAGE_GUIDE.md`.
